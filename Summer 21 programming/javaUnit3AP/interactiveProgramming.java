import java.util.*;


public class interactiveProgramming
{
    public static void main(String[] args)
    {
    Scanner userInput = new Scanner (System.in);
    int a = userInput.nextInt(); //nextInt() is used to read integers
    //double b =userInput.nextDouble(); //nextDouble() is used to read number with decimals

    //String c =userInput.nextLine(); //nextLine() is used to read strings
    if (a%2==0)
    {
        System.out.println("number is even");
    }
    }
}